[package]
name = "viewstamp-compio"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Proactor (compio) driver for viewstamp-proto (implemented in M4)"

[dependencies]
viewstamp-proto = { workspace = true }

[lints]
workspace = true
