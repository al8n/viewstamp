[package]
name = "viewstamp-simulation"
version = "0.0.0"
edition = "2024"
rust-version = "1.95"
repository = "https://github.com/al8n/viewstamp"
license = "MIT OR Apache-2.0"
description = "Deterministic simulation testing harness for viewstamp-proto"
publish = false

[dependencies]
bytes = { workspace = true }
smol_str = { workspace = true }
viewstamp-proto = { workspace = true, features = ["std", "tcp"] }

[lints]
workspace = true
