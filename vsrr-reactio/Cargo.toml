[package]
name = "vsrr-reactio"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Reactor (tokio/async-io) driver for vsrr-proto (implemented in M6)"

[dependencies]
vsrr-proto = { workspace = true }

[lints]
workspace = true
